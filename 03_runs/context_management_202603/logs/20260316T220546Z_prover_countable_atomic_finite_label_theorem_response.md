The finite-label / finite-stratification theorem is still not derivable on the present record.

Write
[
Q_j:=K_j/!\sim
]
for the image of the local witness set (K_j) in the compatibility quotient. The theorem now under test is exactly the claim that the witness-set map (j\mapsto K_j) factors through finitely many compatibility types, equivalently
[
#\Big(\bigcup_{j\in U} Q_j\Big)<\infty.
]
That is the sharpened quotient-level version of the earlier finite-palette question.

What is actually banked is weaker. On the trusted U-side record, one only has coordinatewise witness availability, (\forall j\in U,\ K_j\neq\varnothing), and not the cross-coordinate uniformization statement needed to extract a common compatible witness on an infinite subset. The reviewer-cleared diagnosis is that this is already the first exact failure, and that it occurs before any later aggregation step.

So the first exact reason the finite-label theorem still fails is this: there is no banked theorem sending each admissible local witness to a finite-valued compatibility label determined by already banked local data. The injective incompatibility pattern
[
U=\mathbb N,\qquad K_j={\kappa_j},\qquad \kappa_j\not\sim \kappa_{j'}\ \text{for }j\neq j'
]
remains logically compatible with the current banked inputs. In that pattern, every (K_j) is nonempty, but the quotient image (\bigcup_{j\in U}Q_j) is infinite. Passing to the compatibility quotient does not fix the obstruction; it only collapses witnesses already known to be compatible, and the present record gives no theorem forcing infinitely many coordinates to land in finitely many quotient classes. Thus there is still nothing to pigeonhole.

The durable sources do not add the missing finiteness. They give finite (\Omega), compact metric (\Theta), and the route through the compact convex payoff set (W). But finite-dimensional compact-convex geometry alone does not force finitely many local witness types. Without polyhedrality, finite-face structure, finite active-constraint classification, or an equivalent finite stratification theorem, one can still have infinitely many distinct supporting directions / local cone types. This is consistent with the current proof state, which still lists the best trusted positive theorem as the finite-(M), compact-metric-(\Theta) result, with removal of finiteness of (M) remaining open.

**Needed assumption:** an explicit finite-stratification / finite-label lemma for local witnesses, namely a theorem that every admissible local witness class lies in one of finitely many compatibility classes determined by already banked local data, equivalently that
[
#\Big(\bigcup_{j\in U} K_j/!\sim\Big)<\infty.
]
Without exactly that kind of input, the current record does not rule out an injective quotient image and therefore does not yield an infinite compatible subfamily.

Suggested next local action: formalize (Q_j:=K_j/!\sim) and attempt one explicit lemma that the present local witness construction factors through a finite quotient image (\bigcup_{j\in U}Q_j); if that lemma cannot be proved from current inputs, bank the finite-label step itself as non-derivable on this branch.
