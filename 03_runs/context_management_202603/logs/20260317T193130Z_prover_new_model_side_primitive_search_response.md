## TOP CANDIDATE

No honest candidate. The closest false lead is the countable-atomic “coercive finite-set capture” or “primitive message-coercivity” condition, but the current request is explicitly to find something genuinely earlier than the frozen branch endpoints, and the durable record already says this candidate is only an equivalent reduced-form restatement of the existing tight-sublevel package, not a new primitive.  

## Why it is genuinely new

It is not. The reviewer-cleared countable-atomic notes say the larger alternative-proof pass does **not** produce a genuinely earlier structural condition, and that the closest structural analogue, finite-capture / inf-compactness on selector payoff arrays, collapses back into the same tightness-coercivity content. The finite-case takeaways say the same thing in plainer form: finite `M` matters through adversary-side compactness / minimizer existence, and anything strong enough to restore attainment just repackages the current endpoint.  

## Target branch

If one insisted on naming a nearest candidate, it would target the countable-atomic attainment branch, because that branch’s isolated gap is adversary-side attainment / tightness. There is no comparably plausible model-side primitive in the present record for the exact route. On the exact route, the bottleneck remains the exact version-and-patching saddle lemma: obtain `q*`-almost-everywhere local optimality, then patch the `q*`-null bad set to exact messagewise Bayes optimality without losing adviser-side saddle inequalities against every admissible kernel; the proof state summarizes this exactly as the live bottleneck.   

## Mechanism

The mechanism of the nearest false lead is clear but disqualifying. Coercive finite-set capture yields one tight near-optimal sublevel set; with lower semicontinuity of `V`, that gives attainment of a minimizer `beta*`; once `beta*` exists, the already banked selector/subgradient argument in `X = \prod l^1(M)` and `X^* = \prod l^\infty(M)` recovers rowwise equal-payoff-on-support. That is a valid conditional theorem route. But the same record also says this condition is theorem-equivalent to the present tight-sublevel endpoint, so it fails the requirement of being genuinely earlier.  

## First obstacle

The first exact obstacle is the absence of any model-native assumption, visible on the present record, that forces either of the two missing miracles without assumption smuggling: adversary-side minimizer existence without merely restating tightness/coercivity on the countable-atomic branch, or exact posterior-lift / patchability on the exact route. The general extension notes only surface technical candidates such as topology/compactness, continuity, saddle-point existence, and measurable selection, which are reduction-side rather than native model primitives. So the search space has gone quiet for honest model-side assumptions.  

## Verdict

`NO_HONEST_CANDIDATE`
