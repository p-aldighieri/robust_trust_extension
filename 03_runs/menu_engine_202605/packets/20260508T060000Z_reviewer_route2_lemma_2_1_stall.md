# Reviewer pass — Route 2 Lemma 2.1 stall

You are the **Reviewer**. The Route 2 Lemma 2.1 prover stalled at three named obstructions. Audit the stall: are the obstructions structural (Route 2 R6 dead), or is one of the prover's three unblockers a clean primitive repair?

## Three named obstructions (verbatim from prover)

**(O1) Borel-to-compact deletion gap.** The natural Borel separation produces a primitive deletion functional $A_E(m) := \sum_i \lambda_i \mathbf{1}_{E_i}(m)(v_i - w^*(m))$ that passes the renaming test. But converting Borel $E_i$ to compact $K_i$ is not supplied by ordinary inner regularity: the inner term
$$\int_M\!\inf_{m \in G(s)}\,\sum_i \lambda_i \mathbf{1}_{E_i}(m)\,s\cdot(v_i - w^*(m))\,d\tau(s)$$
is non-monotone in $(E_i)$ because $s\cdot(v_i - w^*(m))$ is signed. Replacing $E_i$ with compact $K_i \subseteq E_i$ may remove the rowwise minimizing contact point and raise or lower the inf unpredictably.

**(O2) Continuum-mass / label-fiber lift gap.** Cell-flow LP variables $x_{rc}$ for mass moved from source cell $B_r$ to message cell $D_c$ cannot in general lift to Borel kernels supported on $R_0$. Two failure modes:
- If $x_{rc}$ is allowed whenever $(B_r \times D_c) \cap R_0 \ne \emptyset$, mass may flow from sources in $B_r$ that have no contact in $D_c$. The matrix is feasible in the LP but cannot be lifted.
- If $x_{rc}$ is allowed only when every $s \in B_r$ has a contact in $D_c$, the LP is too conservative and declares infeasibility where a measurable kernel exists.

**(O3) Slack discipline failure.** $n(\eps)\,\rho_\eps \to 0$ requires the compactification error to be uniform over all dual certificates generated by $V_\eps$. Standing hypotheses don't supply this modulus; the error could scale with vertex count or contact-relation complexity.

The prover's unblockers:
- **(U3)** A new deletion-compatible Hall duality theorem with uniform $o(1/n)$ compact-patch approximation.
- **(U-fiber)** Fiber-thickness or liftability hypothesis on $R_0$.
- **(U-Borel)** Replace $\Delta_\text{del}^{cp}$ with a Borel-patch residual (changes the route's renaming-test surface).

## What you MUST audit

### 1. Are the three obstructions real?

**(O1).** Verify the non-monotonicity of the inner inf. Construct or argue via a concrete toy example. Does inner regularity really fail to bridge the gap, or is there a more sophisticated regularity argument the prover missed?

**(O2).** Verify the two failure modes. In particular, the second mode (LP too conservative) — is it a real concern, or does the union $G(s) \cap D_c$ across sufficiently fine $D_c$ approximate sourcewise contact arbitrarily well? Is there a measurable selection theorem (Aliprantis-Border 18.13/18.19) that bridges this?

**(O3).** Verify the slack scaling. Is there a finite-net regime (e.g., $V_\eps$ at vertices of a polytope W with finitely many vertices) where $n(\eps)$ is bounded and the slack is straightforwardly controlled?

### 2. Are the unblockers viable?

**(U3) deletion-compatible Hall.** This is the closure-memo bottleneck. Confirms that the same theorem unblocks both Route 1 and Route 2.

**(U-fiber).** What does "fiber-thickness or liftability" mean precisely? Is this a primitive condition that passes the renaming test, or does it implicitly invoke calibration content?

**(U-Borel).** Replacing the defect with a Borel-patch residual:
- Does the renaming test still pass with Borel patches instead of compact?
- Does this restore the natural separation without changing the theorem's content?
- Is this just a notational rephrasing or a substantive change?

### 3. Diagnosis: same dragon or different?

The breakdown's worry was that R6 might be Route 1 in disguise. Is this stall confirming that worry, or is there a meaningfully different obstruction?

Specifically:
- Route 1's stall (O1-O4 from previous reviewer) had the killer obstruction (O4) = (H_del) pointwise-strict ≠ uniform.
- Route 2's stall (O1-O3 here) avoids that — quantitative softness handles it. But the Borel-to-compact and label-fiber issues are recognizable as Route-1 family obstructions in different costume.
- Net: is Route 2 R6 strictly weaker than Route 1, or strictly stronger, or incomparable?

### 4. Verdict on Route 2

Three reasonable verdicts:
- **Dead under standing + (exact-contact).** Route 2's forward bound cannot be proved without strictly stronger hypotheses than v8's. The unblockers are scope-narrowing or amount to assuming the conclusion.
- **Alive under (U-Borel).** Replacing the defect with a Borel-patch residual produces a publishable theorem with possibly worse $\Phi$ but a valid forward bound.
- **Alive under (U-fiber) with regularity.** Adds primitive regularity (closed graph, fiber thickness); is publishable as a conditional theorem strictly stronger than v8.

## Verdict and downstream advice

### Verdict levels

- `PASS`: stall is correctly diagnosed; the three obstructions are structural; (U3) is the central bottleneck.
- `PATCH_SMALL`: stall is correct but (U-Borel) is a clean repair the prover undersold.
- `PATCH_BIG`: one of (O1)-(O3) is overstated; Route 2 may live with a tweak.
- `REDO`: prover missed something obvious.

### Downstream advice (one paragraph)

Should the orchestrator:
- **Stop and final gatekeeper.** Both Routes 1 and 2 dead at the closure-memo bottleneck. Take the strategic record to a final gatekeeper for terminal-state verdict.
- **Switch to (U-Borel) variant.** If the Borel-patch defect is a clean repair, run a third breakdown for that variant.
- **Stop with v8.** No further pipeline action.
- **Run the (U-fiber) conditional theorem.** Produce a publishable conditional with regularity hypothesis stronger than v8.

## Output Format

```
\`\`\`review_control
verdict: PASS / PATCH_SMALL / PATCH_BIG / REDO
route_status: viable / blocked / dead
recommended_next_phase: GATEKEEPER / PROVER / BREAKDOWN / SEARCHER / LITERATURE / CONSOLIDATOR / STOP_STALL
proof_status: complete / partial / broken
\`\`\`

## Verdict
VERDICT: ...
Reason: ...

## Detailed Audit
### 1. Three obstructions
- (O1) Borel→compact: real / overstated. Reason: ...
- (O2) Cell-flow lift: real / overstated. Reason: ...
- (O3) Slack discipline: real / overstated. Reason: ...

### 2. Unblockers
- (U3) Deletion-compatible Hall: confirmed central bottleneck. ...
- (U-fiber) Fiber-thickness: viable / scope-narrowing / dead. ...
- (U-Borel) Borel-patch residual: viable / dead / notational. ...

### 3. Same-dragon diagnosis
...

### 4. Route 2 verdict
- Dead under standing + (exact-contact) / Alive under (U-Borel) / Alive under (U-fiber) / Other.

## Opinion and Next Move
(One paragraph. Strong recommendation.)
```

Length: 1500–2000 words.
