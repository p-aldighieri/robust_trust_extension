### Local verdict

On the current record, (U\cup C<\infty) is **neither proved nor refuted**. The reviewer-cleared diagnosis makes bankable that, for
[
S_{\mathrm{ray}}={j\neq i^*: j \text{ is moved at } w^* \text{ and lies in the distinguished } i^*\text{-obstruction ray}},
]
with
[
U={j: j \text{ is not moved at } w^*},\qquad
C={j: j \text{ is moved at } w^* \text{ but not in the } i^*\text{-obstruction ray}},
]
one has
[
\mathbb N\setminus S_{\mathrm{ray}}={i^*}\cup U\cup C.
]
Hence the source-level CTR setup clause (S=\mathbb N\setminus A) with (A) finite is equivalent, for the canonical choice (S=S_{\mathrm{ray}}), to the finiteness lemma
[
U\cup C<\infty.
]
Equivalently, one must prove both (U<\infty) and (C<\infty). 

The first concrete obstruction is exactly the missing bridge from the local ((d_1=e_1)) obstruction witness to an **eventual tail** statement. What is now bankable is that no currently banked lemma yields either eventual movement or eventual single-ray collapse, and that the reviewer-cleared CTR pass only proves a conditional export
[
\mathrm{CTR}\Longrightarrow \text{local no-embedding},
]
starting from an already supplied finite-anchor/infinite-tail decomposition. So CTR does **not** manufacture finiteness of (U) or (C) from the present cone data. This is the earliest failure point; bookkeeping items like (1\in S) and CTR2 are not earlier obstructions. 

### What is bankable

* The canonical definition of (S_{\mathrm{ray}}).
* The identity (\mathbb N\setminus S_{\mathrm{ray}}={i^*}\cup U\cup C).
* The equivalence
  [
  S_{\mathrm{ray}}=\mathbb N\setminus A \text{ for some finite }A
  \quad\Longleftrightarrow\quad
  U\cup C<\infty.
  ]
* The fact that this finiteness lemma is the **first unmet clause** on the countable-atomic direct branch. 

### What is only diagnostic

* That (S_{\mathrm{ray}}) is already cofinite.
* That all but finitely many coordinates are moved.
* That all but finitely many moved coordinates lie in the distinguished (i^*)-ray.
* Any later CTR ingredients attached to that tail. 

### Needed assumptions

The following would settle the clause, but they are **not** currently banked:

1. **Needed assumption:** (U<\infty).
2. **Needed assumption:** (C<\infty).

Under these assumptions one could take
[
A={i^*}\cup U\cup C
]
and conclude (S_{\mathrm{ray}}=\mathbb N\setminus A). Without them, the cofinite-tail setup clause remains open. 

I also do not see a banked branch-compatible counterexample in the attached materials, so the present state is an unresolved local hinge, not a refutation. That matches the durable proof state: the strongest trusted theorem in hand is still only the finite-(M) result, with beyond-finite-(M) work remaining open rather than banked. 

Suggested next local action: prove or refute (U<\infty) and (C<\infty) directly from the countable-atomic branch hypotheses, and do not advance to any later CTR clause until that exact finiteness bridge is settled.
