# Consolidator pass 02 — Apply all verification patches + honest reframing

## Role

You are the Consolidator. The verification block (six fresh-chat
passes: general reviewer, objective conformance, gatekeeper, three
math sanity-check chunks) has returned. Their durable-source responses
are:

- `reviewer_general_response.md` — general integrity audit, PATCH_SMALL
  with explicit fix list.
- `objective_conformance_response.md` — **OBJECTIVE_NARROWED**: not the
  unrestricted standing-only Theorem 2.
- `gatekeeper_response.md` — **OBJECTIVE_NARROWED** confirmed.
- `sanity_chunk1_response.md` — T2 SOUND, T1 + Binary MINOR_FIXES.
- `sanity_chunk2_response.md` — FBNF MINOR_FIXES (FBNF-1 wording).
- `sanity_chunk3_response.md` — G1/G2c/G3 + WTA Ψ=2/9 SOUND. PATCH for
  threshold normalization + scope statements.

## What to produce

A REVISED consolidated document with:

1. **Honest framing**: present the package as a "**strong conditional /
   classification result**", NOT as a proof of unrestricted infinite-M
   Theorem 2 under standing hypotheses alone. The result COMBINES:
   - Unconditional sub-class theorems (binary, FBNF foliated,
     P2*/P3/P4 primitive sufficient classes).
   - The cone-Hall biconditional G3 + LP threshold G4 for the rest.

2. **All PATCH_SMALL fixes applied**:
   - Radon-Nikodym orientation (`dn/dq` not `dq/dn`).
   - R-notation clash between rowwise-minimizer correspondence $R$
     and profile-realization right inverse $R$ (one of them needs
     a different symbol — propose).
   - T1 posterior formula adjustment (sanity chunk 1 flagged).
   - Hypothesis ledger rows for each theorem.
   - Application-table omissions filled.
   - WTA threshold normalization: pick ONE convention and stick to it.
   - Endpoint-fiber (NOT singleton-endpoint) wording throughout
     binary capstone, F1, and G4 LP.
   - FBNF-1: clarify it requires a $\bar\tau$-a.e. Borel affine chart
     / quotient-consistent foliation (per sanity chunk 2).
   - Scope guards tightened across all theorems.
   - "Borel-positive" in Phase (b) restated as "globally continuous
     Bayes-optimal selection".

3. **Sharper hypothesis ledger**: for each theorem, list (Standing /
   Added regularity / Added primitive / Meaningful narrowing /
   Scope-changing) — using gatekeeper's classification.

4. **Sharper open-questions section**: state precisely what remains
   open after this package — including: unconditional unrestricted
   |Ω|≥3 without regularity package, without any primitive class
   condition, and without checkable Ψ(y)≤0.

## Output structure

Same as Consolidator 01 (Sections A through F), but with the
"strong conditional / classification" framing made explicit at the
top, and all PATCH_SMALL fixes applied.

## Output Contract

- Inline markdown.
- LONG document expected (~25-35k chars).
- This is the version intended for Piotr after the user signs off.
- End with: "ready to send to Piotr as a strong conditional /
  classification result".

## Constraints

- Banned tools list applies.
- Do NOT overclaim. The objective+gatekeeper said OBJECTIVE_NARROWED —
  the document must honor that.
- The strong PIECES of math (G1, G2c, G3 biconditional, T1 Clarke-Danskin,
  WTA Ψ=2/9, binary capstone) are SOUND — don't undermine them, just
  position them correctly.
