PASS

The latest prover pass identifies the right first missing ingredient. What is now bankable is that the present record still does not force exact infinitely-many-fiber membership for the concrete set-valued map `j \mapsto K_j`. The banked inputs give only coordinatewise nonemptiness of the fibers, and the earlier reviewer-cleared routes already show that neither the exhausted finite-label route nor the tail-stability / closedness backup upgrades that into a witness lying in `K_j` for infinitely many varying coordinates. In particular, tail compactness yields at most a point in the closure of tail unions, not exact membership in infinitely many fibers.

So the exact obstruction to record is: the first substantive missing lemma is a genuine cross-coordinate recurrence principle for `j \mapsto K_j`, for example a statement of the form `j_n` distinct, `\kappa_n \in K_{j_n}`, `\kappa_n \to \kappa` implies `\kappa \in K_{j_n}` for infinitely many `n`, or any equivalent exact-admissibility-on-a-tail theorem. The separate ambient-normalization hypothesis is only a prerequisite to even formulate a convergence version; it is not the substantive hinge. The prover correctly separates those two issues.

The corrected next local move is therefore narrow and local. Do not move to later aggregation, `C`-side work, or monotone-refinement. Instead, write one exact recurrence lemma for the concrete witness construction `j \mapsto K_j`, with any ambient witness-space normalization stated separately if convergence language is used, and test that lemma directly from the actual definition of `K_j`. If that lemma cannot be proved from the current banked inputs, then exact tail-membership non-derivability should be recorded as settled on this branch. This is consistent with the durable proof state, which still treats the strongest trusted result as the finite-`M`, compact-metric-`\Theta` extension and does not bank any stronger recurrence theorem beyond that point.

Suggested next local action: state the exact missing lemma as a cross-coordinate recurrence statement for `j \mapsto K_j`, record any ambient-normalization requirement separately as a formulation prerequisite, and then check that lemma directly against the concrete definition of `K_j`; if it does not follow, bank exact tail-membership as non-derivable and do not reopen downstream steps.
