PASS.

The scoped negative diagnosis is now correct and bankable on the countable-atomic direct branch.

What is bankable is the following precise negative result. Beyond the settled explicit class, the current obstruction data do **not** manufacture the moved-tail / common-target / QNG package. More concretely, from the presently banked zero-order inputs alone one does **not** get:

[
\text{(i) an admissible infinite moved tail } S,
]

[
\text{(ii) a common target } \bar w\in W \text{ with } \Delta_*:=m_{i^*}\cdot \bar w-c_{i^*}(w^*)>0,
]

or

[
\text{(iii) the strict net-gain sign }
\mathcal N(S,\bar w):=
(1-\alpha)\Big(\tau_{i^*}\Delta_*-\sum_{i\in E_S(w^*)\setminus{i^*}}\tau_i\Gamma_i\Big)-\alpha\Lambda>0,
]
with
[
\Gamma_i:=[c_i(w^*)-m_i\cdot \bar w]*+,
\qquad
\Lambda:=\sum*{j\in S}\tau_j\big(m_j\cdot w_j^*-m_j\cdot \bar w\big).
]

So the current record does **not** yet manufacture QNG outside the explicit class.

This is exactly the right diagnosis given the hard inputs already cleared. The explicit class is dead by a genuine tail-lift contradiction. CTR and QNG are valid **conditional** export lemmas once one has the relevant path-level data. And the reviewer-cleared counterexample really does rule out the naïve replacement “check only exposed rows and keep the old pointwise proof.” The correct contradiction engine is now understood to be: exposed-row linear loss control through (\Gamma_i), plus an (o(t)) weighted tail bound for non-exposed rows, plus the strict first-order inequality (\mathcal N(S,\bar w)>0). That much is solid.

What remains open is a single realization lemma, and it should be stated exactly as such:

**Needed assumption / crux lemma.** There exist an admissible infinite moved tail (S) and a common target (\bar w\in W) such that the active floor is raised,
[
\Delta_*>0,
]
and the quantitative net gain is strictly positive,
[
\mathcal N(S,\bar w)>0.
]

Equivalently: one still has to realize a **QNG-feasible common target** on an admissible infinite moved tail. Everything after that point is already banked; everything before that point is still only diagnostic. In particular, it is still only diagnostic, not proved, that the present ((d_1=e_1))-type obstruction data automatically yield such an (S), such a (\bar w), or the sign of (\mathcal N(S,\bar w)).

Suggested next local action: state and attack exactly the realization lemma “find admissible infinite (S) and (\bar w\in W) with (\Delta_*>0) and (\mathcal N(S,\bar w)>0),” instead of trying to read QNG directly off the zero-order obstruction data.
