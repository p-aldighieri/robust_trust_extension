# Durable Sources

This folder is for bundled source documents used as durable project context in
model runs.

Current durable bundles:

- `phil_reny_bundle.md`: compact source packet for the Phil-Reny route.

Related raw inputs live nearby by role:

- `../primary_paper/`: the primary *Robust Trust* paper.
- `../objective/`: the objective statement and project brief.
- `../external_suggestions/phil_reny/`: Phil Reny's suggestion and supporting
  reference material.

Proof-state sources and route memos now live in `../../02_proof_history/`.
