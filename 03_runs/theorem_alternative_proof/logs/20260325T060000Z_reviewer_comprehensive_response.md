# Reviewer Response — Comprehensive Proof Review (Full Document)

**Chat URL**: https://chatgpt.com/g/g-p-69b612c07c108191a7597062801a020e/c/69c3752f-ce30-832c-a9ab-1751736c84f3
**Role**: reviewer (comprehensive)
**Date**: 2026-03-25

---

Reviewed source and compiled PDF.

Verdict: PATCH_BIG
Section-by-section assessment

Section 3 (Posterior formula, Prop 3)
Pass. The posterior formula and the marginal 
𝑞
𝛽
(
𝑚
)
q
β
	​

(m) are correct in the reduced game where the misaligned adviser is restricted to messages in 
𝑀
=
supp
⁡
(
𝜏
)
M=supp(τ). The proof is complete for that setting. The only caveat is presentational: this should be stated explicitly as the message-level posterior before the agent’s private type is used.

Section 4 (FOC / perturbation)
Mostly pass. The perturbation is the right simplex-preserving row perturbation, and formulas (4)-(5) are correct. Writing things in terms of the unnormalized numerator 
𝑁
𝛽
(
𝑚
,
𝜔
)
N
β
	​

(m,ω) is cleaner than differentiating the posterior ratio directly. The direct/indirect decomposition is sound as intuition, but not yet rigorous as an FOC argument until Sections 5-6 replace ordinary derivatives by one-sided directional derivatives / subgradients.

Section 5 (Envelope theorem, Prop 4)
Pass with one caveat. Milgrom-Segal Theorem 3 is a defensible citation here, and the needed conditions are genuinely satisfied because for fixed 
𝑥
x, 
𝑓
(
𝜀
,
𝑥
)
f(ε,x) is affine in 
𝜀
ε, and 
𝑋
X is compact. The one-sided formulas (6)-(7) are correct. The caveat is the remark that 
Φ
𝑚
Φ
m
	​

 is the maximum of finitely many affine functions indexed by simplex vertices: that is only true if 
𝐴
A is finite, which is not assumed in the paper.

Section 6 (Selector family, Prop 6)
This is the trouble spot.

Step 1 is correct. The optimality condition

0
∈
∂
𝑉
(
𝛽
∗
)
+
𝑁
∏
𝜇
Δ
(
𝑀
)
(
𝛽
∗
)
0∈∂V(β
∗
)+N
∏
μ
	​

Δ(M)
	​

(β
∗
)

is the right convex first-order condition.

Step 2 is not correct as written. The partial derivative 
𝑔
𝑚
(
𝜇
,
𝑚
′
)
g
m
	​

(μ,m
′
) is miscomputed, and the reference to Prop 4 is not enough for the multivariate subgradient claim.

Step 3 is correct once Step 2 is fixed. The normal-cone/KKT condition on each row simplex does yield exactly equations (8)-(9), with the support of 
𝛽
∗
(
⋅
∣
𝜇
)
β
∗
(⋅∣μ) lying in the argmin set.

Section 7 (Commitment game, Prop 8)
Pass conditional on a corrected Prop 6. The LP argument is exactly the right one: once (8)-(9) hold, every message in 
supp
⁡
𝛽
∗
(
⋅
∣
𝜇
)
suppβ
∗
(⋅∣μ) is a row minimizer, so 
𝛽
∗
β
∗
 is optimal in the commitment game.

Section 8 (Minimax, Thm 9)
Mostly pass, but incomplete relative to paper Theorem 2.

(F.1) is correct, conditional on Prop 8.

(F.2) is correct: it uses Bayes-optimality of the selector family message by message, weighted by 
𝑞
𝛽
∗
(
𝑚
)
>
0
q
β
∗
	​

(m)>0.

The connection to Definition 2 is right for the constructed pair 
(
𝜎
∗
,
𝛽
∗
)
(σ
∗
,β
∗
).

But Theorem 9 does not prove the full first sentence of paper Theorem 2. It proves existence of one robustly rationalizable optimal strategy, not that any robustly rationalizable strategy is optimal.

Errors found

[GAP] Prop 6, Step 2 computes 
𝑔
𝑚
(
𝜇
,
𝑚
′
)
g
m
	​

(μ,m
′
) incorrectly.
As written, the formula leaves 
𝜔
ω unsummed and suggests nonzero “other-message contributions.” The correct derivative is

∂
∂
𝛽
(
𝑚
′
∣
𝜇
)
∑
𝜔
𝑢
ˉ
(
𝜎
^
𝑚
∗
,
𝜔
)
𝑁
𝛽
(
𝑚
,
𝜔
)
=
1
{
𝑚
′
=
𝑚
}
(
1
−
𝛼
)
𝜏
(
𝜇
)
∑
𝜔
𝑢
ˉ
(
𝜎
^
𝑚
∗
,
𝜔
)
𝜇
(
𝜔
)
.
∂β(m
′
∣μ)
∂
	​

ω
∑
	​

u
ˉ
(
σ
^
m
∗
	​

,ω)N
β
	​

(m,ω)=1
{m
′
=m}
	​

(1−α)τ(μ)
ω
∑
	​

u
ˉ
(
σ
^
m
∗
	​

,ω)μ(ω).

So for 
𝑚
′
≠
𝑚
m
′

=m, it is exactly 
0
0.

[GAP] Prop 6, Step 2 does not justify the selector-family representation properly.
A general subgradient of a max-function is usually a convex combination of active gradients. To conclude that one may use a single Bayes-optimal selector 
𝜎
^
𝑚
∗
σ
^
m
∗
	​

 for each message 
𝑚
m, the proof must explicitly use that the private-strategy set is convex and the payoff is linear in mixed private strategies. Then convex combinations of active gradients are again generated by active mixed strategies.

[GAP] Prop 6, Step 2 cites the wrong tool.
Prop 4 gives one-dimensional one-sided derivative formulas. Step 2 needs the multivariate subgradient formula for a supremum of affine maps, i.e. a standard Danskin / max-of-affine convex-analysis statement. Prop 4 is compatible with that story, but it is not by itself the needed justification.

[GAP] Theorem 9 is not the full theorem being advertised.
The paper’s Theorem 2 says: “Any robustly rationalizable strategy is optimal. If 
𝑀
M and 
Θ
Θ are finite, a robustly rationalizable strategy exists.”
Theorem 9 only proves the second part plus existence of one optimal robustly rationalizable 
𝜎
∗
σ
∗
. The first part needs a short extra converse argument.

[ASSUMPTION+] The document silently restricts to 
𝛼
∈
(
0
,
1
)
α∈(0,1).
The source repeatedly uses 
𝑞
𝛽
(
𝑚
)
≥
𝛼
𝜏
(
𝑚
)
>
0
q
β
	​

(m)≥ατ(m)>0. That excludes the endpoint 
𝛼
=
0
α=0. If the theorem is meant to cover endpoints, they need to be handled separately.

[GAP] Reduced-game versus full-game strategy space is not fully reconciled in the theorem statement.
The proof works with the reduced adversary space 
𝛽
:
𝑀
→
Δ
(
𝑀
)
β:M→Δ(M). Theorem 9 then switches to 
𝛽
∈
𝐵
β∈B without defining 
𝐵
B or explaining how off-
𝑀
M messages are treated. This is patchable, but it needs to be said.

[GAP] The abstract/executive summary currently overstate the result.
Given Error 1 and Error 4, the document is not yet a complete verified proof “as written,” even though the overall proof strategy looks salvageable.

Notation/typo issues

Σ
Σ and 
𝐵
B are used in Theorem 9 but are not defined in the document.

The remark after Prop 4 saying “pointwise maximum of finitely many affine functions (indexed by vertices of the strategy simplex)” is false unless 
𝐴
A is finite. The paper only assumes 
𝐴
A compact metric.

In Prop 6, Step 2, the notation 
𝑋
∗
(
0
)
X
∗
(0) is awkward/misleading in a multivariate 
𝛽
β-argument. It should be something like 
𝑋
𝑚
∗
(
𝛽
∗
)
X
m
∗
	​

(β
∗
).

The document oscillates between the simplified 
𝑢
(
𝑎
,
𝜔
)
u(a,ω) notation and the full 
𝑢
ˉ
(
𝜎
^
,
𝜔
)
u
ˉ
(
σ
^
,ω) notation. This is manageable, but it makes the exposition feel more welded than seamless.

In the proof of Prop 3, the sentence “Decompose 
Pr
⁡
𝛽
(
𝑚
∣
𝜔
)
=
𝛼
⋅
1
[
aligned
]
+
(
1
−
𝛼
)
⋅
1
[
misaligned
]
Pr
β
	​

(m∣ω)=α⋅1[aligned]+(1−α)⋅1[misaligned]” is sloppy. What is really being decomposed is the joint probability contribution, not a literal equality of conditional probabilities.

Numerical cross-references are fine after a normal rerun. I did not find broken equation/proposition labels. The real problem is logical support, not LaTeX numbering.

Net-net: Sections 3, 4, 5, and 7 are basically in good shape. Section 8 is close, but it is missing the universal “any robustly rationalizable strategy is optimal” half. Section 6 needs a substantive rewrite of Step 2 before the document can be called correct.