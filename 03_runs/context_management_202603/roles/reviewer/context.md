# Reviewer Context

## Status

This is the recommended first active role for the current state.

## Primary Local Sources

- the exact prover output under review
- the active breakdown
- the current route memo
- `Context Management/source_notes/proof_state.md`

## Immediate Review Task

Review the exact prover artifact that is being passed, not the whole project history.
For the current relaunch, the first task is to rerun review on the full `main` and `route_2` prover artifacts because the previous reviewer packets were truncated.

## Output To Maintain

- verdict
- exact broken step
- exact reusable step
- recommended next role

## Scope Rule

- Review one prover artifact or one amended proof block at a time.
- If the packet includes too much history, ask for a narrower review target instead of trying to summarize everything.
