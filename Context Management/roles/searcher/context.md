# Searcher Context

## Status

This is one of the default starting roles for the current project state.
It is not the current recommended restart point when a late-stage branch already exists.

## Primary Local Sources

- `objective_statement.md`
- `Context Management/source_notes/proof_state.md`
- current trustworthy route memo
- current trustworthy breakdown or reviewer note if route choice depends on it

## Current Search Priority

1. Decide among currently live routes using only trustworthy artifacts.
2. Identify the exact crux lemma or topology bottleneck.
3. Kill weak routes early.
4. Keep the route memo narrow enough that a later breakdown can be manually inspected.

## Output To Maintain

- ranked route list
- failure points
- recommended branch names
